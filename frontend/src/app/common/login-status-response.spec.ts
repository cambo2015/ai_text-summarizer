import { LoginStatusResponse } from './login-status-response';

describe('LoginStatusResponse', () => {
  it('should create an instance', () => {
    expect(new LoginStatusResponse()).toBeTruthy();
  });
});
